fn sequence() {
    r := Rect{}
    r.set_position(Vec2{0, 0.6});
    // r.set_size(Vec2{0.1, 0.1});
    // r.set_color(Color{255, 20, 255, 255});
    add_obj(&r);
    printf("%v\n", r);

    // tx := Text{}
    // tx.position.y = 0.2;
    // tx.text = "Hello, world";
    // add_obj(&tx);
    // printf("%v\n", tx);

    fade_in(r.id);
    // fade_in(tx.id);
    play(2);
}
