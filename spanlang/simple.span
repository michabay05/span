scene Test {
    prim r1 = rect();
    color c = red;
    if position(r1) == [0.5*vw, 0.2*vh] {
        c = blue;
    }
    translate(r1, [0.5*vw, 0.2*vh], 0.5, rate=linear);
    prim t1 = text("5 in.");
    rotate(t1, 90deg);
}

